{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --reload",
    "start": "uvicorn main:app",
    "build": "echo 'No build step needed for Python'"
  }
}
